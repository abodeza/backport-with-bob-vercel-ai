---
'@ai-sdk/harness-opencode': patch
---

Write OpenCode harness host tool schemas to a bridge-state file instead of embedding them in the MCP environment, and honor `permissionMode: 'allow-all'` when building OpenCode permissions.
